version: '3.8'
services:
  shortener:
    build: .
    ports:
      - "8000:8000"
    environment:
      - BASE_URL=http://localhost:8000
      - DB_PATH=/data/urls.db
    volumes:
      - data:/data
volumes:
  data: